# Automation Anywhere Community Edition Credentials
# Replace these with your actual registered credentials
USERNAME=your-username-here
PASSWORD=your-password-here

# Application URLs
# BASE_URL should not include hash routes (#/login) or query parameters
BASE_URL=https://community2.cloud-2.automationanywhere.digital
API_BASE_URL=https://community2.cloud-2.automationanywhere.digital/api

# Browser Configuration
# Set HEADLESS=true for CI/CD pipelines, false for local debugging
HEADLESS=false

# Slow down operations (in milliseconds)
# Useful for debugging - slows down each action by specified milliseconds
# Set to 0 for normal speed, increase (e.g., 1000) for debugging
SLOW_MO=0

# Optional: Timeout Settings (in milliseconds)
# These can be configured here or left to use defaults from playwright.config.js
# ACTION_TIMEOUT=60000
# NAVIGATION_TIMEOUT=90000
# TEST_TIMEOUT=120000
